package com.calendarApp;

import io.dropwizard.Configuration;

public class CalendarConfiguration extends Configuration {
    // not required yet, but later on can be used to configure while extending the problem
}